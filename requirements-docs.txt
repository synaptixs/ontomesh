# Documentation build dependencies.
# Used by `mkdocs build` locally and by .github/workflows/docs.yml in CI.
#
#   pip install -r requirements-docs.txt
#   mkdocs serve

mkdocs-material>=9.5.0
mkdocs-material[imaging]>=9.5.0   # social-card auto-rendering
pymdown-extensions>=10.7
pillow>=10.3.0
cairosvg>=2.7.1
mike>=2.0.0                       # versioned docs (latest, 3.7.x, etc.)
